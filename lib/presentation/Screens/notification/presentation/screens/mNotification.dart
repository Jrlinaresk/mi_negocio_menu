import 'package:flutter/material.dart';

class mNotification extends StatelessWidget {
  const mNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pantalla de notificaciones"),
    );
  }
}